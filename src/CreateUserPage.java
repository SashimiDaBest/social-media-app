import java.awt.*;
import javax.swing.*;
public class CreateUserPage extends JFrame {
    public CreateUserPage() {
        setLayout(new BorderLayout());
        JLabel label = new JLabel("Welcome to Page 2.1");
        add(label, BorderLayout.CENTER);
    }
}
