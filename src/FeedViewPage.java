import javax.swing.*;
import java.awt.*;

public class FeedViewPage extends JPanel {
    public FeedViewPage() {
        setLayout(new BorderLayout());
        JLabel label = new JLabel("Welcome to Page 2.2");
        add(label, BorderLayout.CENTER);
    }
}
